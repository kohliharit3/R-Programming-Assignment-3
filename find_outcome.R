find_outcome <- function(outcome) {
        
        if (outcome == "heart attack") {
                
                return (as.numeric(11))
        
        } else if (outcome == "heart failure") {
                
                return (as.numeric(17))
                
        } else if (outcome == "pneumonia") {
                
                return (as.numeric(23))
        }
        
}